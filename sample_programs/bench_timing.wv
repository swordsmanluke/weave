# Simple timing benchmark with different scales
fn run_baseline(iterations) {
    count = 0
    i = 0
    while i < iterations {
        count = count + 1
        i = i + 1
    }
    puts count
}

puts "Running 100 iterations..."
run_baseline(100)

puts "Running 1000 iterations..."
run_baseline(1000)