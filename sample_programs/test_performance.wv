# Test closure performance with hot loops
fn test_hot_loop() {
    count = 0
    
    fn increment() {
        count = count + 1
    }
    
    # Hot loop - 2500 iterations
    i = 0
    while i < 2500 {
        increment()
        i = i + 1
    }
    
    puts count  # Should be 5000
}

test_hot_loop()
